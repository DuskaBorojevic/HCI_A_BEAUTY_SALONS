<Window x:Class="HCI_A.Windows.CustomYesNoMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CustomYesNoMessageBox"
        Width="450" Height="250"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        ShowInTaskbar="False">

    <Border CornerRadius="12"
            Background="{DynamicResource SecondaryLightBrush}"
            BorderBrush="{DynamicResource PrimaryBrush}"
            BorderThickness="2"
            Padding="20"
            Effect="{DynamicResource MaterialShadow}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleTextBlock"
                       Grid.Row="0"
                       Text="{DynamicResource ConfirmDeletion}"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextOnPrimaryBrush}"
                       VerticalAlignment="Top"
                       HorizontalAlignment="Left"/>


            <TextBlock x:Name="ContentTextBlock"
                       Grid.Row="1"
                       TextWrapping="Wrap"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextOnPrimaryBrush}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"/>


            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,20,0,0">
                <Button Content="{DynamicResource BtnYes}"
                        Width="100" Height="40"
                        Style="{DynamicResource SecondaryTextButton}"
                        Margin="0, 0, 5, 20"
                        Click="YesButton_Click"/>
                <Button Content="{DynamicResource BtnNo}"
                        Width="100" Height="40"
                        Style="{DynamicResource SecondaryTextButton}"
                        Margin="5, 0, 0, 20"
                        Click="NoButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
