<Window x:Class="HCI_A.Windows.ServiceTypesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HCI_A.Windows"
        mc:Ignorable="d"
        Title="{DynamicResource ServiceTypes}" Height="400" Width="500"
        FontFamily="{DynamicResource DefaultFont}"
        Background="{DynamicResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20,15,20,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{DynamicResource ServiceTypesLabel}" FontSize="18" FontWeight="SemiBold" 
                       VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>

            <Button x:Name="AddButton" Grid.Column="1"
                    Style="{StaticResource AddButton}" Click="AddButton_Click"/>
        </Grid>


        <Rectangle Grid.Row="0" Height="1" VerticalAlignment="Bottom" 
                   Fill="{DynamicResource PrimaryBrush}" Margin="20,0"/>

        <ScrollViewer Grid.Row="1" Margin="20,10,20,20" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ServiceTypesPanel">
                <StackPanel.Resources>
                    <Style TargetType="FrameworkElement">
                        <Setter Property="Margin" Value="0,5,0,5"/>
                    </Style>
                </StackPanel.Resources>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>