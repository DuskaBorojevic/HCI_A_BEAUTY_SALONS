<Window x:Class="HCI_A.Windows.BeauticianMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HCI_A.Windows"
        xmlns:controls="clr-namespace:HCI_A.Controls"
        mc:Ignorable="d"
        Title="{DynamicResource BeautcianMainPageTitle}" 
        Height="700" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        FontFamily="{DynamicResource DefaultFont}"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Background="{DynamicResource PrimaryBrush}" 
                Effect="{DynamicResource MaterialShadow}">
            <Grid>
                <StackPanel Orientation="Horizontal" 
                            VerticalAlignment="Center"
                            Margin="25,0">
                    <TextBlock x:Name="WelcomeLabel" 
                               Foreground="White"
                               FontFamily="{DynamicResource SecondaryFont}"
                               FontSize="22" 
                               Margin="20,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,25,0">

                    <!--Theme and Language Selector-->
                    <controls:ThemeLanguageSelector Margin="0,0,15,0"/>

                    <Button x:Name="ProfileButton"
                            Style="{DynamicResource IconButtonStyle}"
                            Width="40"
                            Height="40"
                            Margin="0,0,15,0"
                            Click="ProfileButton_Click"
                            ToolTip="{DynamicResource MyProfileTooltip}">
                        <Path Data="{DynamicResource UserIcon}" 
                              Fill="White" 
                              Stretch="Uniform" 
                              Width="22" 
                              Height="22"/>
                    </Button>

                    <Button x:Name="LogoutButton"
                            Style="{DynamicResource IconButtonStyle}"
                            Width="40"
                            Height="40"
                            Click="LogoutButton_Click"
                            ToolTip="{DynamicResource LogoutTooltip}">
                        <Path Data="{DynamicResource LogoutIcon}" 
                              Fill="White" 
                              Stretch="Uniform" 
                              Width="22" 
                              Height="22"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <TabControl x:Name="MainTabControl" 
                    Grid.Row="1" 
                    Margin="10"
                    Background="Transparent"
                    BorderThickness="0">
            <TabItem Header="{DynamicResource SalonInfoTab}">
                <ScrollViewer>
                    <StackPanel x:Name="SalonInfoContent"/>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="{DynamicResource ScheduleTab}">
                <StackPanel x:Name="ScheduleContent"/>
                <!--<ScrollViewer>
                    <StackPanel x:Name="ScheduleContent"/>
                </ScrollViewer>-->
            </TabItem>

            <TabItem Header="{DynamicResource ServicesTab}">
                <ScrollViewer>
                    <StackPanel x:Name="ServicesContent"/>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="{DynamicResource ProductsTab}">
                <ScrollViewer>
                    <StackPanel x:Name="ProductsContent"/>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="OrdersTabControl" Header="{DynamicResource OrdersTab}">
                <ScrollViewer>
                    <controls:OrdersControl x:Name="OrdersControlInstance"/>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
